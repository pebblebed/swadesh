__pycache__/
*.pyc
*.part

# Bulk derived datasets (~72 MB each): deterministically regenerated from data/raw
# + the scripts, so we don't bloat the repo with them. Run the scripts to rebuild.
# The compact derived artifacts (glossary.tsv, *_review.tsv, *summary.tsv) ARE tracked.
data/normalized/swadesh.jsonl
data/normalized/ipa.jsonl
data/normalized/ipa_features.jsonl
data/normalized/asjp.jsonl
# practical tier per-record review is large (~2.3 MB, 42k rows) and regenerable;
# the tracked summary is metadata/practical_ortho_report.tsv (per list).
data/normalized/ipa_practical_review.tsv

# machine-local Claude Code session settings (permissions, etc.)
.claude/settings.local.json
